import { FlattenPipePipe } from './flatten-pipe.pipe';

describe('FlattenPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FlattenPipePipe();
    expect(pipe).toBeTruthy();
  });
});
